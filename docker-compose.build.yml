services:
  backend:
    build:
      context: ./backend
    pull_policy: never

  web:
    build:
      context: ./web
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://127.0.0.1:3000}
    pull_policy: never
