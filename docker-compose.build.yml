services:
  backend:
    build:
      context: ./backend
    pull_policy: never

  web:
    build:
      context: ./web
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
    pull_policy: never
