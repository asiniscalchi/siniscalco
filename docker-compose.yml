services:
  backend:
    image: ${BACKEND_IMAGE:-ghcr.io/${GHCR_OWNER:-asiniscalchi}/siniscalco:${APP_TAG:-latest}}
    env_file:
      - ./backend/.env.example
    ports:
      - "3000:3000"
    volumes:
      - backend_data:/app/data

volumes:
  backend_data:
