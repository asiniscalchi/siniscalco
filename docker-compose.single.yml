services:
  app:
    build:
      context: .
      args:
        GIT_VERSION: ${GIT_VERSION:-}
    image: ${APP_IMAGE:-siniscalco:${APP_TAG:-local}}
    env_file:
      - ./backend/.env.example
    ports:
      - "8080:80"
    volumes:
      - app_data:/app/data

volumes:
  app_data:
